# Required
DATABASE_URL=postgres://user:password@localhost:5432/popcornvault?sslmode=disable

# Optional
SERVER_PORT=8080
FETCHER_USER_AGENT=PopcornVault/1.0
FETCHER_TIMEOUT=30s

# Optional — Semantic search (VoyageAI)
# If VOYAGE_API_KEY is not set, the app runs without semantic search.
VOYAGE_API_KEY=
